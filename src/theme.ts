import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#9575cd",
    },
    secondary: {
      main: "#4a0072",
    },
    background: {
      default: "#e0e0e0",
    },
  },
});

export default theme;
