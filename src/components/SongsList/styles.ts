import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  list: { display: "grid", rowGap: "1rem" },
  albumView: {
    rowGap: "1rem",
  },
});

export default useStyles;
