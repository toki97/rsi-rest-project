import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  list: { display: "grid", rowGap: "2rem" },
  albumView: {
    rowGap: "1rem",
  },
});

export default useStyles;
